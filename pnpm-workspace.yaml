# Both ship platform binaries via postinstall scripts, and pnpm 11 requires
# build scripts to be allow-listed explicitly: esbuild is what Vite builds
# with, workerd is the runtime `wrangler dev` runs the sync hub on.
allowBuilds:
  esbuild: true
  workerd: true
